{
  "name": "root",
  "private": true,
  "devDependencies": {
    "lerna": "^3.22.1"
  },
  "workspaces": [
    "packages/*"
  ],
  "command": {
    "publish": {
      "ignoreChanges": [
        "packages/vue-next/**"
      ]
    }
  },
  "version": "independent",
  "scripts": {
    "sfDemo": "yarn workspace sfDemo serve"
  }
}
